import { createWalletClient, parseAbi } from "viem";
/**
 * Function should be called from the frontend which sends form data, which is preprocessed here before being passed to the blockchain, making a call to the blockchain with its passed parameters, returns bool and error message
 */
export default function createDao(): boolean | Error {
}




