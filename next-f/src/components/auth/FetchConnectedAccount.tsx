/**This function takes in the account connected from the inAppWallet or external wallet & returns it as both provider & signer capable of signing transactions on behalf of user**/
export async function FetchConnectedAccount() {
  /** useActiveWallet hook to fetch current user signer **/
return (
   <>
       <h1>Fetch Connect Account </h1>
   </>
  )
}
